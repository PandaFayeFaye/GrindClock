# GigTime（牛马打卡机 / GrindClock）设计系统

版本：v1.2 · 更新日期：2026-09-16 · 基于已确认的视觉方向：**高饱和插画风（分层降噪版）**
参考实现：[牛马变装秀 - 视觉方向对比](https://claude.ai/code/artifact/fc32636a-f1b2-49f9-865b-24bdc528164e)

**本版本更新**：App端与微信小程序端均已按此设计系统完成开发并上线/进入体验版测试。心情标签（第9节）实际做了8档而非文档曾经描述的4档，已同步更新；新增第13节记录小程序端实现这套设计系统时踩到的平台特定坑，供后续维护/其他平台移植参考。

---

## 1. 核心原则：两级视觉强度

这套设计系统最重要的规则不是颜色本身，而是**按场景分配视觉强度**，避免"厚描边+硬投影"在高频页面造成疲劳：

| 强度等级 | 使用场景 | 特征 |
|---|---|---|
| **日常层（Quiet）** | 打卡列表、设置项、明细列表等每天反复看/操作的内容 | 细描边（1.5px）、无硬投影、低饱和背景 |
| **主角层（Bold）** | 今日收入卡、核心CTA按钮、悬浮操作按钮 | 3px黑色描边 + 硬投影（offset shadow，无模糊） |
| **仪式层（Poster）** | 月度战绩分享卡、升级/成就弹窗、新雇主入职动画 | 满格强度：撞色背景、贴纸装饰、大号圆体标题 |

**决策依据**：一个元素在一屏里出现的频率越高，强度必须越低；用户主动触发、低频、有情绪峰值的时刻，强度可以拉满。

---

## 2. 色彩系统

### 2.1 基础色板

| Token | 色值 | 用途 |
|---|---|---|
| `bg-base` | `#FBF7EC` | 页面主背景（暖白，比纯白降低疲劳感） |
| `bg-surface` | `#FFFFFF` | 卡片/列表行背景 |
| `border-quiet` | `#DDD6C2` | 日常层描边（1.5px） |
| `border-bold` | `#1A1A1A` | 主角层/仪式层描边（3px），同时也是主文字色 |
| `text-primary` | `#1A1A1A` | 主文字 |
| `text-muted` | `#1A1A1A` at 60% opacity | 次要信息（时薪、时间戳） |

### 2.2 品牌撞色（用于主角层与仪式层）

| Token | 色值 | 用途 |
|---|---|---|
| `accent-yellow` | `#FFD93D` | 主色，今日收入卡背景、称号徽章底色 |
| `accent-blue` | `#4361EE` | 主要操作按钮（如"上班打卡"） |
| `accent-coral` | `#FF6B6B` | 进行中/强调状态（如"下班打卡"进行中）、悬浮操作按钮 |
| `accent-shadow-yellow` | `#FFB800` | 仪式层专属，分享卡CTA投影色 |

### 2.3 雇主标识色板（多雇主场景专用）

系统预设6色循环分配给新添加的雇主，用户可在这6色内手动改选，不开放自定义调色盘（保持整体色彩克制）：

```
#FFD93D（黄）  #4361EE（蓝）  #FF6B6B（珊瑚红）
#39C97A（绿）  #B084F5（紫）  #5AC8FA（浅蓝）
```

这套色标贯穿雇主列表色块、统计图表堆叠色、明细列表——**同一个雇主在任何页面都用同一个颜色**，是多雇主场景保持可读性的关键规则。

### 2.4 语义色（仅用于状态提示，不作为装饰色使用）

| Token | 色值 | 用途 |
|---|---|---|
| `semantic-success` | `#39C97A` | 保存成功提示、同步完成 |
| `semantic-warning` | `#FFB800` | 检测到跨天班次等需要确认的提示 |
| `semantic-error` | `#E14B4B` | 表单校验错误 |

---

## 3. 字体系统

### 3.1 字体家族（三种角色分工，都通过Google Fonts加载）

| 角色 | 字体 | 使用场景 |
|---|---|---|
| **展示字体** | `ZCOOL KuaiLe`（圆体加粗中文展示字） | 大号数字（今日收入）、页面主标题、分享卡标题——扛情绪冲击力的地方 |
| **正文字体** | `Noto Sans SC`（400/500/700/900四档字重） | 所有UI文案、按钮文字、列表内容 |
| **数据字体** | 数字统一用 `font-variant-numeric: tabular-nums`（等宽数字），配合Noto Sans SC 700 | 金额、时长、日期——保证多行数字对齐 |

**加载方式**：
```html
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700;900&family=ZCOOL+KuaiLe&display=swap" rel="stylesheet">
```

### 3.2 字号阶梯

| Token | 字号 | 字重 | 用途 |
|---|---|---|---|
| `text-display` | 28-32px | ZCOOL KuaiLe | 今日收入数字、分享卡标题 |
| `text-h1` | 19-20px | Noto Sans SC 700 | 页面/横幅标题 |
| `text-h2` | 15-16px | Noto Sans SC 700 | 卡片小标题、雇主名称 |
| `text-body` | 13-14px | Noto Sans SC 400/500 | 正文、按钮文字 |
| `text-caption` | 11-12px | Noto Sans SC 400 | 次要信息、时间戳、标签 |

行高统一 1.4-1.5，标题类文字加 `text-wrap: balance` 防止孤字换行。

---

## 4. 间距系统

采用 **4px基准网格**，只使用以下几档，禁止任意数值：

```
4 / 8 / 12 / 16 / 20 / 24 / 32 / 40px
```

| 场景 | 间距 |
|---|---|
| 卡片内边距 | 16px（日常层）/ 18-22px（主角层与仪式层，留白更宽松突出重要性） |
| 列表行之间 | 10px |
| 卡片之间（模块间距） | 16-20px |
| 页面左右边距 | 16px（对应移动端安全边距） |
| 图标与文字间距 | 8px |

---

## 5. 圆角与投影系统

| Token | 圆角 | 投影 | 使用层级 |
|---|---|---|---|
| `radius-quiet` / `shadow-none` | 14px | 无 | 日常层（列表行、次要卡片） |
| `radius-bold` / `shadow-bold` | 16px | `5px 5px 0 #1A1A1A`（硬投影，无模糊） | 主角层（收入卡、悬浮按钮） |
| `radius-poster` / `shadow-poster` | 24px | `6px 6px 0 [强调色]` | 仪式层（分享卡、成就弹窗） |
| `radius-pill` | 999px | 无 | 标签/徽章（如称号、streak） |

**按钮按下态**：投影从 `2px 2px 0` 收到 `0`，同时元素位移 `translate(2px, 2px)`，模拟物理按压——这是本系统区别于普通扁平风的标志性交互细节，所有可点击的主角层元素都要有这个状态。

---

## 6. 描边系统

| Token | 粗细 | 使用场景 |
|---|---|---|
| `border-quiet` | 1.5px，色值 `#DDD6C2` | 日常层卡片 |
| `border-bold` | 2.5-3px，色值 `#1A1A1A` | 主角层、仪式层卡片，以及所有图标 |

---

## 7. 图标系统

现有方向的插画/吉祥物走"厚描边+扁平色块"风格，图标需要统一到同一视觉语言，避免和精细线性图标混用导致风格割裂：

| 规则 | 说明 |
|---|---|
| 风格 | 厚描边（2-2.5px）+ 扁平填色，不用渐变、不用细线图标 |
| 网格 | 24×24px 基准网格设计，实际使用时按 18/20/24/32px 等比缩放 |
| 圆角 | 图标内部转角统一用 2px 圆角，和整体"贴纸感"呼应 |
| 色彩 | 图标默认用 `text-primary`（#1A1A1A）描边+品牌撞色填充，禁止使用灰阶图标（会显得和整体风格脱节） |
| 底部导航图标 | 3个（首页/统计/我的），选中态填充 `accent-yellow`，未选中态描边灰 `#8A8A8A` |
| 图标来源建议 | 优先自绘保证描边粗细统一；若用现成图标库，选类似 [Phosphor Icons Duotone] 或 [Iconoir Bold] 这类支持自定义描边粗细的库，二次加粗描边到2.5px |

---

## 8. 吉祥物规范

| 规则 | 说明 |
|---|---|
| 造型 | 戴安全帽的卡通小牛，圆形头部+椭圆耳朵/牛角+椭圆口鼻，厚黑描边+撞色填充 |
| 尺寸 | 首页横幅用 46-48px，成就徽章/空状态插画可放大到 96-120px |
| 表情状态 | 至少需要3套表情：😊开心（默认/收入达标）、😐平常（默认打卡态）、😫疲惫（深夜打卡/连续高强度工作提示） |
| 动效 | 待机呼吸动效（轻微上下浮动+旋转，2.6s循环），必须包在 `@media (prefers-reduced-motion: no-preference)` 内，尊重系统设置 |
| 禁止 | 不做写实渲染、不做3D质感，保持和图标同一套"厚描边扁平"语言 |

---

## 9. 数据可视化系统

统计页不能只有柱状图——调研发现现有工时类App的统计可视化普遍薄弱（多为列表+汇总数字），这是明确的差异化机会（对应FEATURE_SPEC 3.4）。以下是本产品的图表类型规范：

| 图表类型 | 使用场景 | 视觉规则 |
|---|---|---|
| 柱状图 | 短期趋势（近7天/本周） | 按雇主颜色堆叠，遵循雇主标识色板（第2.3节） |
| 热力日历 | 月度活跃度总览 | 类似GitHub贡献图，格子颜色深浅映射当天工时/收入强度；空白格=没打卡的一天，不用负面色（如红色）标记，保持中性灰 |
| 环形目标进度 | 用户自设的收入/工时目标 | 环形描边用主角层强度（3px黑边），中心大数字用展示字体；达成100%触发庆祝动效（见9.1）+ 撞色变化（环形从紫色变金色） |
| 雇主横向排行榜 | 多雇主对比 | 条形+名次徽章（自绘图标，不用emoji数字），呼应"竞技排位"的游戏化调性；条形颜色沿用雇主标识色 |
| 连续打卡streak日历 | 展示打工火苗连续天数 | 复用热力日历网格，改为二值状态，连续格子之间用小火苗连线视觉串联 |
| 心情曲线 | 统计页/我的页"本周心情曲线"小卡片 | 折线连接7个心情点，配4条参考网格线；**实际做了8档心情标签**（见FEATURE_SPEC 3.14，比本文档早期版本描述的4档更丰富），每档专属手绘表情图标+固定色值（色值表见FEATURE_SPEC 3.14），不用红黄绿这类过于情绪化的信号色，保持和整体撞色系统一致 |

**图表配色统一原则**：所有图表的颜色语言必须复用已定义的雇主标识色板和品牌撞色（第2节），不额外引入图表专用配色，保持全App视觉体系统一。

**叙事化呈现**：月度/周度的总结类图表（对应FEATURE_SPEC 3.9月度战绩总结）走"故事卡片"形式而非表格——数据配一句具象化类比文案（见FEATURE_SPEC.md第5章文案示例库），而非单纯罗列数字，这是从Spotify Wrapped得到的验证经验：叙事感来自"揭晓感+文案"而不是图表本身的复杂度。

### 9.1 数据可视化动效

| 场景 | 时长/缓动 |
|---|---|
| 环形进度条填充 | 页面进入后600ms ease-out，从0动画到实际百分比 |
| 目标达成庆祝 | 环形短暂放大回弹（scale 1→1.08→1）+ 撞色渐变，<500ms |
| 热力日历格子进入 | 逐格交错淡入，每格延迟20ms，总时长不超过400ms |

以上动效均需包在 `prefers-reduced-motion: no-preference` 内。

---

## 10. 动效与反馈

| 场景 | 时长/缓动 | 说明 |
|---|---|---|
| 按钮按下态 | 80ms ease | 投影收起+位移，见第5节 |
| 页面/卡片进入 | 200-260ms ease-out，从下方12px处滑入+淡入 | 用于打卡确认卡片、弹窗 |
| 吉祥物待机动效 | 2.6s ease-in-out 循环 | 见第8节，需遵守 reduced-motion |
| 数字滚动 | 打卡确认后金额从0滚动到实际值，400ms | 增加"入账"的爽感反馈，仅用于金额类数字 |
| 打卡音效（可选，默认开启，设置可关） | 轻快的"叮"声，<300ms | 不能造成操作延迟感 |

---

## 11. "简洁模式"对设计系统的影响

用户在设置中开启"简洁模式"后：

- 吉祥物、称号横幅、streak标签、数据具象化文案、打卡音效**全部隐藏/关闭**
- 主角层的硬投影和撞色**保留**（这是核心视觉识别，不属于"花活"），但仪式层的分享卡/成就弹窗改为按需查看，不主动弹出
- 图标、字体、间距、圆角系统**不变**——简洁模式关闭的是"情绪化内容"，不是整个设计系统

---

## 12. 双语（中/英）设计规范

产品要做中英文可切换，以下内容是中文版设计系统之外需要补充的规则。

### 12.1 展示字体：中英文不能共用一个字体文件，但要"神似"

`ZCOOL KuaiLe` 是纯中文字库，不含拉丁字母（或字形残缺），**不能**直接给英文内容做fallback。需要单独选一个英文展示字体，气质上匹配"圆润+饱满+有力量"这个感觉，而不是随便拿个粗体英文字体凑数：

| Token | 中文 | 英文 | 说明 |
|---|---|---|---|
| `font-display-zh` | `ZCOOL KuaiLe` | — | 中文展示场景 |
| `font-display-en` | — | `Fredoka`（Google Fonts，圆润饱满的展示字体，重量感和圆体感与中文选字气质一致） | 英文展示场景 |

**判断标准不是"长得像"，而是"给人的情绪一致"**——两种字体的字形完全不同是正常的，只要粗细比例、圆角程度、视觉重量在切换语言时感觉是"同一个产品"，就是对的。

### 12.2 正文字体：可以共用

`Noto Sans SC` 本身包含完整的拉丁字符集，和其他语言版本的Noto Sans家族设计一致，**中英文正文可以直接共用这一套字体**，不用切换，只需要在极少数需要"纯正英文排版感"的场景（如果未来做英文站的营销页）才考虑换成 `Inter` 或 `Work Sans` 这类专门的英文字体。

### 12.3 字体切换的实现方式

用 `<html lang="zh">` / `<html lang="en">` 或语言状态类名驱动CSS变量切换，不要在每个组件里写 if/else 判断字体：

```css
:root {
  --font-display: var(--font-display-zh);
}
html[lang="en"] {
  --font-display: var(--font-display-en);
}
```

### 12.4 文案长度差异——最容易被忽略的坑

同样的意思，中文短语通常比英文短很多。比如称号"卷王候选人"（5个字）翻译成英文可能是"Grind King Candidate"（20+字符），如果称号标签、按钮、Tab文字用了**固定宽度**设计，英文版会直接溢出或被截断。

**规则**：
- 所有标签/徽章/按钮用 `padding` + 内容自适应宽度，禁止写死 `width`
- 底部Tab导航这类空间紧张的地方，英文文案要单独设计缩短版（如"Stats"而不是"Statistics"），不能指望自动换行/缩小字号解决
- 设计稿阶段两种语言都要单独过一遍关键页面，尤其是称号横幅、streak标签、打卡按钮这几个文字量差异最大的地方

### 12.5 文案要"本地化"，不是"翻译"——这点对本产品尤其重要

第5章"文案示例库"里的梗（牛马、卷王、摸鱼、搬砖）是中文互联网特有的自嘲文化，直译成英文会完全失去笑点甚至显得奇怪（"Work Ox King"对英语用户没有任何梗的联想）。英文文案需要**用英语世界对应的打工人自嘲文化重新写一遍**，而不是找人翻译中文文案。参考方向：英语区类似的自嘲梗来自 "corporate humor" "hustle culture吐槽" "r/antiwork" 这类语境。

| 场景 | 中文 | 英文（本地化重写，非直译） |
|---|---|---|
| 首页收入类比 | "本月已搬砖87小时，相当于连续刷了29部电影" | "You've clocked 87 hours this month — that's a full season of your comfort show, twice over." |
| 深夜打卡 | "卷到这么晚，牛马也要注意身体" | "Grinding this late? Even the office ox needs sleep." |
| streak中断 | "火苗熄了，牛马也要按时上班哦" | "Streak's out. Even hustle culture takes a nap sometimes." |
| 净收益对比 | "你的实际到手时薪是¥18.3/小时，比隔壁摸鱼的猫少一点" | "Your real hourly rate is $18.30 — a little less than what the office cat makes doing nothing." |
| 新雇主添加成功 | "新雇主 [名称] 入职啦" | "New gig unlocked: [Name] 🎉" |
| 称号体系 | 萌新打工人→摸鱼练习生→搬砖能手→卷王候选人→牛马之王 | Rookie Grinder → Slack-Off Trainee → Shift Veteran → Grind Candidate → Ultimate Work Ox |
| 月度总结标题 | "本月打工战绩报告" | "This Month's Grind Report" |
| 空状态 | "还没有雇主？点击右下角开始你的搬砖之旅" | "No gigs yet — tap below to start the hustle" |

**建议**：这部分英文文案找母语级别的人重新创作或至少做一轮本地化审校，机翻/直译会直接毁掉这个产品最大的情感差异化亮点。

### 12.6 图标/吉祥物资产必须"文字无关"

吉祥物、图标严禁把文字直接画进图片/SVG里（比如把"打卡"两个字画在图标上），所有文字必须是独立的、可被i18n框架替换的文本节点。图标本身（安全帽小牛、打卡符号等）是语言中立的视觉符号，中英文版本完全复用同一套图标资产，不需要重画。

### 12.7 数字/货币/日期格式

| 内容 | 规则 |
|---|---|
| 货币符号 | 跟随用户实际所在地区/自选币种，不要跟着界面语言走（有人用英文界面但收入是人民币） |
| 数字分隔符 | 中文/英文环境通用阿拉伯数字+千分位逗号，无需切换 |
| 日期格式 | 中文默认"9月11日"，英文默认"Sep 11"，用 `Intl.DateTimeFormat` 按 locale 处理，不要手写日期拼接逻辑 |
| 技术实现建议 | React生态推荐 `react-i18next` 或 `next-intl` 做文案管理，货币/日期统一走 `Intl` 系列API，不要自己拼字符串 |

---

## 13. 微信小程序端落地这套设计系统的平台差异说明

小程序端（Taro + WXSS）整体完整复用了本设计系统（色板、字体角色分工、间距/圆角/投影分级、图标风格、吉祥物、数据可视化图表类型），以下是几处因平台限制导致实现方式不同、但视觉效果保持一致的地方：

| 项目 | App端（Web） | 小程序端 | 说明 |
|---|---|---|---|
| 展示字体加载 | `<link>`标签加载Google Fonts | 字体文件在构建时转成base64字符串内联进代码，运行时用`wx.loadFontFace`注册 | 小程序不能像网页一样直接链接外部字体CSS；本地包内文件路径给`loadFontFace`也不可靠（详见PRD第2.2节踩坑记录），只有内联base64是稳定的 |
| 环形进度图表（第9节目标环形进度） | SVG `<circle>` + `stroke-dasharray` | CSS `conic-gradient` | 小程序WXSS不支持内联SVG，用conic-gradient画环形是等效替代方案 |
| 成就墙节点连接线（第9节的延伸，规划外新增的"称号进阶之路"，见FEATURE_SPEC 3.10） | 可直接用SVG path | 运行时测量容器像素宽高，用旋转定位的矩形div模拟线段 | 同样是"无内联SVG"限制下的替代方案 |
| 顶部导航栏标题字体/字号 | 不受限（就是普通网页标题） | **不能自定义**，微信原生导航栏字体/字号是平台固定的，唯一能改的是背景色和文字黑/白二选一 | 如果某个页面必须要用展示字体做标题（如仪式层场景），需要把该页面整个换成`navigationStyle: "custom"`自绘导航栏，牺牲原生返回箭头/胶囊按钮的默认处理，两者取舍需按页面重要性individually决定，不建议全局都换 |
| 头像图片自定义裁剪位置（如"露出人物上半身"效果，对应第8节吉祥物/头像类需求） | CSS `object-fit: cover; object-position: 50% 18%` | 只能用原生`<image mode="aspectFill">`（居中裁剪，不能自定义裁剪锚点） | 小程序`<image>`组件不支持`object-position`等效属性；曾尝试用`background-image`模拟，在开发者工具模拟器正常但真机上完全不显示图片，已废弃改回`mode="aspectFill"` |
| 按钮/卡片的点击态反馈（第5节"按钮按下态"、通用tap-active缩放） | CSS `:active`伪类 | 原生组件的`:active`不可靠，改用`hoverClass`属性绑定一个自定义class | WXSS的`:active`在`view`/`button`等原生组件上表现不稳定，`hoverClass`+`hoverStayTime={0}`是官方推荐的等效替代 |
| 底部Tab导航 | 常规路由高亮 | 完全自定义组件（`tabBar.custom: true`），因为要做本设计系统要求的"浮动深色胶囊+每个Tab独立强调色+选中态弹跳动画"效果，原生TabBar做不到 | 副作用：原生的`wx.hideTabBar()`对自定义TabBar不生效，任何需要遮住底部的全屏弹窗必须让自定义TabBar组件自己监听一个显隐事件来配合 |

以上均为"实现手段不同、视觉规范一致"，不代表设计系统本身有平台特殊版本——任何未来的新页面/组件设计，仍然按第1-13节的规范来，只是小程序端的工程实现要留意上表列的平台限制。

---

## 14. 待设计师/开发确认的开放项

- 图标是自绘一套完整图标库，还是基于现成库二次改造描边粗细，需要评估设计资源投入
- 雇主色板是否需要在深色模式/护眼模式下做单独调整（当前系统未设计深色模式，仪式层的分享卡本身就是深色，可作为深色模式的美学参考起点）
- 吉祥物的3套表情+待机动效具体交给谁绘制/实现（Lottie动画 vs CSS/SVG手写），影响开发成本
- 英文文案由谁负责本地化创作（而非翻译）——建议找英语母语者或至少做本地化审校，机翻直接会毁掉产品的情感差异化亮点
