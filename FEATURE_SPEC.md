# GigTime（牛马打卡机 / GrindClock）— 详细功能与交互规格说明书

版本：v1.0 · 更新日期：2026-09-16
用途：供UI/交互设计与后续开发直接参考的字段级、屏幕级规格——**本版本已把下方大部分章节从"设计规划"更新为"实际实现"**，与当前App端+小程序端上线的版本一致；仍标注为"待开发"的项目是真实缺口，其余均已完成（部分实现细节与最初规划有出入，以本文档为准，具体差异点在对应小节内注明）。

---

## 0. 产品调性与游戏化设计

### 0.1 为什么要做这个方向

调研中反复出现的现有产品共性问题：功能是标准表格/日历，界面严肃工具化，没有情绪价值。而目标用户是自嘲"打工牛马""社畜""搬砖人"的年轻人——这套网络语言本身就是一种情绪出口和身份认同。把这种情绪接住、用幽默化解，是市场上所有竞品都没做的情感差异化，比多加一个功能更容易让人愿意每天打开、愿意分享出去。

### 0.2 核心设计原则

1. **自嘲不emo**：文案可以玩梗（牛马、卷王、搬砖），但整体基调是"苦中作乐"，不能真的显得压抑或贩卖焦虑
2. **数据要有画面感**：不要只显示数字，用类比把工时/收入"翻译"成具体的生活场景（见0.4）
3. **输入要像玩游戏，不要像填表**：能用选卡片/滑动/点图标解决的，不用下拉菜单和长文本框
4. **克制使用，别过度玩梗到影响可用性**：核心记录流程3步内必须能完成，梗和动画是"调味"不是"主菜"，设置里提供"简洁模式"开关给不想要花活的用户关掉

### 0.3 视觉与角色设定

- **产品吉祥物**：一只戴着安全帽/工牌的卡通小牛（呼应"牛马"梗），作为空状态插画、加载动画、成就徽章的贯穿视觉角色，比抽象图标更有记忆点和传播性
- **称号体系**（根据累计工时/打卡天数自动升级，展示在"我的"页顶部）：
  「萌新打工人」→「摸鱼练习生」→「搬砖能手」→「卷王候选人」→「牛马之王」
  —— 纯装饰性称号，不影响任何功能，但给"打开App看今天升级了没"一个理由
- **打卡连续天数**：做成"打工火苗"streak（类似多邻国的连续打卡火焰），断了会有调侃文案"火苗熄了，牛马也要按时上班哦"
- **称号升级阈值**（按累计工时，取账号内所有雇主+代记录数据总和）：

| 称号 | 累计工时门槛 |
|---|---|
| 萌新打工人 | 0小时（默认） |
| 摸鱼练习生 | 满10小时 |
| 搬砖能手 | 满50小时 |
| 卷王候选人 | 满200小时 |
| 牛马之王 | 满500小时 |

  这套阈值是MVP起点，上线后可根据实际用户分布数据调整节奏（不宜升级过慢导致用户长期停在"萌新"，也不宜过快失去激励感）。

### 0.4 数据具象化文案（贯穿统计页/首页）

把冷冰冰的数字换算成有画面感的类比，示例：
- "本月已搬砖87小时，相当于连续刷了29部电影"
- "这周多雇主合计赚了¥520，够加15次奶茶"
- "你的实际到手时薪是¥18.3/小时，比隔壁摸鱼的猫少一点"（配合3.5净收益对比页）
- 深夜打卡（22点后）触发特殊文案："卷到这么晚，牛马也要注意身体"
- 月末生成"打工战绩总结"卡片，样式参考"支付宝年度账单"但走自嘲搞笑路线，一键生成可分享到微信/小红书的长图——**这是最重要的传播抓手，建议作为P1优先实现**

### 0.5 输入交互简化方向（对应第3章各表单）

- 结算模式选择：用**图标卡片横滑选择**代替下拉菜单（时薪/日结/底薪+加班/综合工时/月结/按单计费，各配一个自绘图标卡片，点击即选中，不用打开二级菜单）
- 时薪/金额输入：提供**常用数字快捷按钮**（¥20/¥25/¥30等根据历史输入自动推荐），减少每次都要手动敲数字
- 打卡确认卡片：加一个**心情标签快速选择**（详见3.14，可跳过），用于后续生成"这个月最累的一天/最爽的一天"这类趣味统计，输入成本几乎为零但能喂给数据具象化文案
- 加班/节假日倍率：不用数字输入框，改成**预设倍率标签**点选（1.5倍/2倍/3倍/自定义），大多数场景一键搞定

### 0.6 需要克制的地方（避免"为了有趣而有趣"）

- 打卡按钮本身必须保持**一键直达**，任何动画/音效不能拖慢这个核心动作的响应速度
- 严肃数据（如导出报表、税务相关）保持克制专业的呈现，不玩梗，避免用户对着报税单笑不出来
- 提供"简洁模式"设置项，一键关闭所有动画/称号/文案彩蛋，服务不想要这些的用户

---

## 1. 竞品功能拆解（屏幕/字段级）

### 1.1 中国市场

**小时工记账**
- 首页：日历视图为核心，每日格子显示当天工时/收入摘要；横滑切换月份；"今天"按钮回到当前月；顶部"计算工时"入口进入月度汇总
- 打卡：①点击开始计时（自动记录，进行中状态显示）②手动补录（选日期直接填工时数字，系统按时薪×工时自动算日薪）
- 结算模式：小时工 / 综合工时 / 正班+加班 / 底薪+加班 / 日结，五选一；可为"不同工厂"分别设置多档时薪
来源：[小米应用商店](https://app.mi.com/details?id=com.julangling.xsgjz)

**极简记工时**
- 打卡模式：固定工时打卡 / 灵活上下班打卡自动算时长
- 字段设计：时薪 + 加班费率 + "补贴与扣款"独立字段（人性化，基础工资外可加/减金额）+ 加班备注（事由/项目名）
- 统计：月度/年度/自定义时段三种周期；支持Excel导出
来源：[应用宝](https://sj.qq.com/appdetail/cn.jianyun.worktime)

**工时记录-记加班考勤助手**
- 四种薪资计算方式：正班+加班 / 底薪+加班 / 综合工时制 / 小时计算
- 两种记录方式："工时记录"（直接填时长）与"时间记录"（填上下班时间点自动算时长）
来源：[App Store](https://apps.apple.com/cn/app/id1498461623)

**喔趣（企业考勤参照，非个人工具，仅供打卡交互参考）**
- 首页大按钮居中打卡，下方班次信息卡片；WIFI/GPS/二维码/指纹/人脸/蓝牙多种打卡方式可切换
来源：[App Store](https://apps.apple.com/cn/app/id1035110131)

**中国产品共性发现**：普遍缺乏图表可视化统计（多为列表+汇总数字），"按雇主/项目筛选统计"的界面几乎没有公开说明——**这是可视化差异化的机会点**。均未见"跨天班次"专门处理逻辑，是空白。

### 1.2 海外市场

**Toggl Track**
- 底部导航：Timer / Calendar / Reports 三个Tab
- 主界面：Suggestions（常用任务快捷开始）+ Time Entry Log（按天分组历史）+ Favorites Bar（收藏条目一键复用）+ 右下角悬浮Play按钮
- 创建/编辑条目（底部弹层）：Description输入框（支持`@项目名` `#标签`语法）、Project下拉（含"+ Create Project"）、Client下拉（含"+ Create Client"）、Tags多选、Billable开关、Start/End时间选择器、Duration显示（HH:MM）、时间轮盘微调
- **Client→Project两层结构**：建Project时选/建Client，这是"添加雇主"页面的直接参考模板（雇主=Client，工作类型=Project，我们可以简化成一层）
来源：[Toggl iOS Timer文档](https://support.toggl.com/toggl-track-timer-for-ios)

**Everlance（自动检测行程）**
- 后台GPS自动检测行程开始，无需手动点按钮；结束后生成待分类条目，**左右滑动分类**（右滑=Work，左滑=Personal），系统学习常走路线自动分类未来行程；自动检测总开关放在首页可一键关闭
来源：[Everlance帮助中心](https://help.everlance.com/hc/en-us/articles/15680330271757)

**Gridwise（收入聚合仪表盘）**
- 卡片式布局：今日/本周收入、在线时长、高峰时段提示、排队等候信息
- "Weekly Recap"周报卡片：总收入、最赚钱平台、最佳工作日、里程、效率、支出汇总
来源：[Gridwise官网](https://gridwise.io/)

**Connecteam（雇主端薪资规则设置）**
- 路径：员工详情 → Employment Tab → Set Pay Rate；Company Policies → Overtime & Pay Rules → Add Policy（规则类型选overtime/额外时薪，设倍率）；Company Policies → Breaks → Add Break（名称、手动/自动、带薪/不带薪、时长）
来源：[Connecteam Pay Rates](https://help.connecteam.com/en/articles/6091866-pay-rates)

**Toggl Reports（报表页）**
- 左上角Date Selector（预设区间/自定义）+ Add Filter多维度筛选 + 柱状图（悬停饼图切片看细节）+ 右上角Export（PDF/CSV/Excel）
来源：[Detailed Report](https://support.toggl.com/en/articles/10750394-detailed-report)

---

## 2. 我们产品的核心亮点（相对竞品的差异化定位）

| 亮点 | 说明 | 对应竞品空白 |
|---|---|---|
| **多雇主原生聚合统计** | 一个仪表盘看所有雇主的合并工时/收入，而非各雇主数据孤立 | 中外产品普遍围绕单一雇主/项目设计 |
| **图表可视化统计** | 柱状图/趋势图 + 按雇主筛选/切换的组合 | 中国产品统计普遍是列表+数字，缺乏可视化 |
| **手动补录+自动打卡并存** | 覆盖"忘记打卡"场景 | 已是中国产品共性优点，我们要保留 |
| **限时段隐私友好定位** | 仅工作时段生效，可完全关闭 | Hubstaff式持续监控引发反感 |
| **免费透明的基础能力** | 导出、多雇主数量、云同步均不设付费墙 | 极简记工时"项目限5个""强制充会员"被骂惨 |
| **AI辅助录入**（拍照识别/语音） | 降低多雇主场景下的记录摩擦 | 中外均无此类功能 |
| **团队代记工时** | 组长/包工头可为多人批量记工 | 中外产品全部空白 |

---

## 3. 屏幕清单与详细字段规格

### 3.0 登录 / 首次启动引导页（Onboarding & Login）——已实现

| 组件 | 说明 |
|---|---|
| 启动页 | 吉祥物动画Logo + 产品名，2秒内自动跳转，不做强制的多页开屏广告/介绍 |
| 登录方式 | 两个Tab切换：**手机号**（默认，中国用户优先）/ **邮箱**（默认，海外用户优先，根据系统语言自动选中对应Tab）。两种都是免密码：手机号→输入手机号→发送短信验证码→输入6位验证码完成登录；邮箱→输入邮箱→点击"发送登录链接"→跳去邮箱App点击链接→自动跳回本App完成登录（Capacitor需配置深链接接住这个跳转）。下方小字放隐私政策链接 |
| 邮箱登录的"检查邮箱"等待态 | 发送链接后停留在一个等待页："已发送登录链接到 xxx@xxx.com，去邮箱点一下吧" + "重新发送"按钮（60秒倒计时防止连续点击） |
| 首次登录后引导（3屏轻量卡片，可跳过） | ①"这是什么"：一句话说明多雇主聚合记工 ②"添加你的第一份工作"：直接引导进入3.2添加雇主表单，不做纯说明性空转页面 ③完成后直接进首页，首页此时是空状态（见3.11） |
| 语言选择 | 首次启动根据系统语言自动选中英文，设置页可随时手动切换（对应DESIGN_SYSTEM.md第11章） |

**原则**：引导流程越短越好，目标是尽快让用户添加完第一个雇主并完成第一次打卡，而不是走完一遍"功能介绍"。

### 3.1 首页 / 打卡页（Home）——已实现

**布局参考**：Toggl的悬浮Play按钮 + 小时工记账的日历/今日摘要 结合

| 组件 | 说明 |
|---|---|
| 顶部：吉祥物+称号横幅 | 卡通小牛形象 + 当前称号"卷王候选人" + 打工火苗streak天数，点击可展开当日趣味文案（见0.4） |
| 今日/本周合并收入卡片 | 大字号显示"今日已赚 ¥xxx"，下方一行数据具象化类比文案（如"够加3杯奶茶"），点击可切换类比口味 |
| 雇主快捷打卡列表 | 每个雇主一行：色块（雇主专属颜色）+ 雇主名 + 时薪 + 右侧"上班打卡"/"下班打卡"大按钮（进行中状态显示计时秒表动画+轻微呼吸光效） |

**多雇主并发打卡规则（重要，纠正早期实现的bug）**：只禁止**同一个雇主**重复打卡，**允许不同雇主同时处于"打卡进行中"状态**——网约车/外卖骑手"多平台同时挂着接单"是真实高频场景，全局互斥会直接违背多雇主并行记录这个核心卖点。合并统计里"总工时"字段需要相应处理重叠时间（见3.4的统计口径说明），避免出现"一天工作36小时"这种反直觉数字。
| 悬浮操作按钮 | "+ 添加雇主"，长按弹出快捷菜单（拍照识别/语音记工/补录工时） |
| 底部导航 | 首页 / 统计 / 我的（3个Tab，对应Toggl的Timer/Reports/Settings精简版） |

**简洁模式**：设置页可一键关闭吉祥物、称号、streak、类比文案，首页退化为纯净的雇主列表+打卡按钮，服务不想要花活的用户（对应0.6）

**手动补录入口**：首页顶部"+"菜单或每个雇主行的"..."菜单里提供"补录工时"，跳转到补录表单（见3.3）

### 3.2 添加/编辑雇主页（Employer Form）——已实现

参考Toggl的Client字段 + 中国产品的结算模式设计，交互上按0.5的原则做游戏化简化：

| 字段名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 雇主名称 | 文本 | ✅ | 输入时下方实时预览雇主卡片长相 |
| 颜色标签 | 色块横滑选择 | ✅ | 系统预设色板自动分配，点色块切换，不做调色盘 |
| 结算模式 | **图标卡片横滑选择**（6选1）：时薪 / 日结 / 底薪+加班 / 综合工时 / 月结 / 按单计费 | ✅ | 点击卡片即选中并展开对应字段，不用下拉菜单。**"综合工时"MVP阶段简化处理**：不实现中国劳动法"综合计算工时工时制"的法定加班周期判定（规则复杂、因地区/行业而异），选中后按"时薪"同等方式计算，仅作为展示标签区分统计口径，加班/节假日倍率字段仍可选填 |
| 基础时薪 | 数字输入 + **常用金额快捷按钮**（¥20/¥25/¥30，根据历史输入动态推荐） | 时薪模式必填 | |
| 日结金额 | 数字输入 + 快捷按钮 | 日结模式必填 | |
| 底薪 | 数字输入 | 底薪+加班模式必填 | |
| 月薪 | 数字输入 | 月结模式必填 | 适合坐班型兼职（如驻场客服）；打卡仍正常记录工时用于统计出勤天数，但收入不随工时波动 |
| 每单价格 | 数字输入 + 快捷按钮 | 按单计费模式必填 | 适合外卖/网约车骑手等按件计酬场景；打卡记录"在线时长"，实际收入按（单价 × 完成单数）计算，完成单数在打卡确认卡片/补录表单里填写（见3.3新增字段） |
| 加班费率倍数 | **预设倍率标签点选**：1.5倍/2倍/3倍/自定义 | 可选 | 参考Connecteam的Overtime Policy设计，但去掉手动输入门槛 |
| 节假日费率倍数 | 预设倍率标签点选 | 可选 | |
| 默认补贴/扣款规则 | 正负数字，可加多条，每条配一个emoji分类（🎁补贴/💸扣款） | 可选 | **仅用于固定的规律性调整**（如"每次夜班固定补贴20元"），会自动套用到该雇主每一条新记录上。**临时性的一次性补贴/扣款**（如某天额外发的奖金）在打卡确认卡片/补录表单里单独填（见3.3），不在这里 |
| 休息扣除时长 | 预设时长标签（30分钟/1小时/不扣除） | 可选 | 参考Connecteam的Break规则 |
| 结算周期 | 单选：日结/周结/月结 | 可选 | |
| **净收益对比设置**（可折叠，默认收起） | 预估通勤时长（分钟）+ 预估通勤交通费 + 预估等待/摸鱼时间占比（如骑手接单间隙） | 可选 | 一次性设置，供3.5净收益对比页计算"实际到手时薪"使用；折叠展示避免干扰大多数不需要这个功能的用户 |
| 备注 | 文本 | 可选 | |

保存后弹出一个轻松的确认动画（小牛举牌"新雇主 [名称] 入职啦"），而不是静默跳转返回。

### 3.3 打卡 / 手动补录表单——已实现

**自动打卡流程**：点击"上班打卡"（可配一声轻松打卡音效，如"叮"或小牛哞叫彩蛋，简洁模式下关闭）→ 按钮变为"下班打卡"+进行中秒表 → 点击"下班打卡" → 弹出确认卡片：显示本次时长+预估收入（大字号，带一句数据具象化文案，如"这单够买2杯奶茶"）+ **心情标签快选**（详见3.14，可跳过）+ **本次补贴/扣款**（可选，一次性调整，如临时奖金）+ 可编辑备注 → 保存。**核心动作（点击打卡按钮本身）响应必须即时，动画/音效不能造成任何延迟感**（对应0.6）

**并发规则**：可以同时对多个不同雇主处于"打卡进行中"，App不做阻止；对同一个雇主重复点"上班打卡"则不生效（按钮已处于"下班打卡"态）。

**手动补录字段**：
| 字段 | 说明 |
|---|---|
| 雇主 | 下拉选择 |
| 日期 | 日期选择器 |
| 记录方式 | 切换Tab："直接填时长" / "填上下班时间点"（自动算时长）—— 参考"工时记录-记加班考勤助手"的双模式设计 |
| 时长或起止时间 | 依上方选择变化 |
| 是否加班/节假日 | 开关，触发对应倍率计算 |
| 完成单数 | 仅当雇主结算模式为"按单计费"时显示，数字输入，收入=单价×这个数字 |
| 本次补贴/扣款 | 可选，一次性调整，与雇主默认规则（3.2）分开单独记录 |
| 心情标签 | 可选，同自动打卡流程，见3.14 |
| 备注 | 文本 |

**跨天班次处理**（现有产品的空白，我们的补充设计）：若下班时间早于上班时间，自动判定为跨天班次，UI提示"检测到跨天班次，是否按次日计算？"用户确认后正确拆分统计到两个自然日或算作一条完整记录（产品需决策，建议：默认算入开始打卡的那一天，统计页脚注说明）

### 3.4 统计/报表页（Stats）——已实现

参考Toggl Reports + Gridwise Weekly Recap 结合：

| 组件 | 说明 | 状态 |
|---|---|---|
| 顶部日期范围选择器 | 今日/本周/本月/全部四档 | ✅ |
| 雇主筛选 | 标签式多选筛选（多雇主时才显示），可只看某几个雇主 | ✅ |
| 合并汇总卡片 | 本时段/累计总工时+总收入（跨雇主相加）；加班时长/加班收入单独一张汇总卡（有加班才显示） | ✅ |
| 柱状图（趋势Tab） | 近7天每日收入柱状图，配当日工时；**新增：支持左右翻页/滑动查看任意历史7天区间**（不只是"最近7天"），标题会切换成具体日期范围 | ✅（含规划外新增的翻页能力） |
| **热力日历** | 月度收入热力日历，颜色深浅=当天收入强度，点击某天展开当日工时/收入详情；**新增：支持翻页查看历史月份**（不能翻到未来） | ✅ |
| **目标环形进度** | 用户可设置本周收入目标（点击文字直接编辑），环形进度+中心百分比数字，采用CSS conic-gradient（小程序端）/ SVG描边（App端）两种实现；目标文案旁展示"目标¥X，已赚¥X"+本周工时 | ✅ |
| **雇主横向排行榜** | 条形排行榜，按本周收入排名，条形长度按最高者归一化，第一名特殊配色；**混合币种时排行榜下方有免责说明**"不同币种之间没有做汇率换算，排名仅供参考" | ✅ |
| **连续打卡streak日历** | 复用热力日历同一份月份状态的二值日历，打卡的日子显示白色火焰图标 | ✅ |
| 明细列表 | 按日期倒序，每条显示雇主色块、名称、时长、收入（含加班子行），**点击任意一条可直接跳转编辑该记录**（App端在建；小程序端已实现，复用补录表单预填数据） | ✅（小程序端）/ 🟡（App端待补） |
| 导出按钮 | 弹出面板：**新增自定义日期范围筛选**（不受页面当前所选时段限制，可选任意起止日期，含实时匹配条数）+ 字段勾选 + 导出CSV/图片两种格式；**新增导出内容末尾自动生成汇总行**"累计X天 · X小时 · ¥X" | ✅（未做PDF，图片格式可覆盖PDF的分享场景） |
| **打工战绩总结卡片**（月度总结独立入口） | 见3.9独立屏幕说明 | ✅ |
| **首页快捷跳转**（新增，规划外） | 首页的"今日/本周已赚"和"本月已赚"两张卡片可直接点击，跳转到本页并自动选中对应的时间范围 | ✅ |

**"总工时"统计口径说明**（对应3.1的多雇主并发打卡规则）：由于允许不同雇主同时打卡，直接把各雇主时长相加会在并行工作的时间段上重复计入，可能出现"今天工作36小时"这类反直觉数字。规则：**首页/统计页的"总工时"按自然时间去重计算**（多个雇主同时段工作只算一次物理时长），但**每个雇主各自的工时展示**和**总收入计算**不受影响、仍按各自实际时长独立计算——即"钱可以来自并行的两份工作各自结算，但'工时'这个字段说的是你人生里花掉的物理时间"。此规则需在开发时对应实现为区间合并（interval merge）算法，而非简单sum。

### 3.5 净收益对比页——已实现

| 组件 | 说明 |
|---|---|
| 雇主列表 | 按"实际到手时薪"从高到低排序 |
| 每行展示 | 雇主名 + 名义时薪 + 预估通勤成本（用户可手动填一次性设置：通勤时长+交通费）+ 实际到手时薪 |
| 说明文案 | "实际时薪已扣除预估通勤时间和费用，仅供参考" |

### 3.6 我的 / 设置页（Profile & Settings）——已实现

| 分组 | 项目 |
|---|---|
| 顶部个人卡片 | 头像 + 当前称号（萌新打工人→...→牛马之王）+ 累计工时/streak天数展示，点击查看完整成就徽章墙 |
| 账户 | 登录邮箱/头像、退出登录 |
| 打卡设置 | 定位打卡开关（默认关闭）、限定生效时段说明文案 |
| 数据 | 导出全部数据、云同步状态显示 |
| AI功能 | 拍照识别开关、语音记工开关（P1功能） |
| **趣味设置** | "简洁模式"总开关（关闭吉祥物/称号/streak/类比文案，见0.6）、打卡音效开关 |
| 关于 | 版本号、隐私政策链接 |

### 3.7 拍照识别 / 语音记工入口（AI功能）——已实现

- 首页"+"菜单增加两个入口：📷"拍照识别排班/账单" 和 🎙️"语音记工"
- 拍照流程：调用相机/相册 → OCR识别 → 展示识别结果草稿（雇主、日期、时长/金额均可编辑）→ 确认保存
- 语音流程：按住说话（参考微信语音交互）→ 转文字 → AI解析出结构化草稿 → 确认保存

### 3.8 团队代记工时（组长/包工头场景）——已实现

**角色模型决策**（对应PRD开放问题）：不做完整的多用户权限系统，MVP阶段用"轻量档案"方案——

| 概念 | 说明 |
|---|---|
| 代记录人（Worker Profile） | 组长账号下创建的"人员档案"，无需独立注册/登录，只有姓名+可选备注，类似一个"影子雇员" |
| 数据归属 | 代记录人的所有工时记录归属于组长账号，组长可查看/编辑/删除；如果这个人后续想自己独立使用App，需要单独注册一个真实账号，两边数据不合并（避免做账号合并/迁移这类复杂功能） |

| 屏幕/组件 | 说明 |
|---|---|
| 入口 | "我的"页新增"团队"入口（只有组长模式下可见，首次使用时引导用户选择"个人模式"还是"团队模式"，可随时切换） |
| 人员列表页 | 类似首页的雇主列表，改为"代记录人"列表，每人一行：姓名+今日/本周累计工时+"+ 记一笔"按钮 |
| 为某人记工 | 复用3.3的打卡/补录表单，顶部增加"为谁记录"的人员选择器 |
| 团队统计页 | 复用3.4统计页结构，增加"按人员筛选"维度，可看到"谁本周工时最多""团队总工时"等汇总 |
| 添加代记录人表单 | 字段：姓名（必填）、备注（可选，如工种/联系方式）、默认时薪（可选，会预填到之后的记工表单里） |

**数据隔离原则**：团队代记的数据（`workerId`不为空的记录）与组长自己的个人打卡记录（`workerId`为空）完全分开展示——首页/统计页/月度总结/称号体系等个人向内容只统计组长自己的记录，团队数据只在"团队"入口下的独立列表/统计里出现，两者不混合汇总，避免组长自己的"卷王候选人"称号被手下人的工时不小心刷上去。

### 3.9 月度战绩总结（独立屏幕，最主要的传播抓手）——已实现，含彩纸掉落动效等规划外细节

不再只是统计页里的一个入口，作为独立的全屏体验设计：

| 组件 | 说明 |
|---|---|
| 触发时机 | 每月1号自动生成上月总结，首页横幅出现"你的[X月]战绩报告出炉啦"提示条；用户也可在统计页手动查看历史月份的总结 |
| 内容结构 | 标题（"这个月，你搬了X小时的砖"）+ 四宫格数据卡（合计收入、最长streak、最赚钱雇主、最累的一天）+ 称号徽章（当前称号+距离下一级还差多少） |
| **"最累的一天"兜底规则** | 心情emoji是可选可跳过的，若本月没有任何一天标记过😫，则改用"当天总工时最长的一天"作为兜底，卡片文案自动切换为"这天你干得最久"而不是硬套"最累"的措辞，避免数据缺失时卡片显示空白或报错 |
| 底部操作 | "生成分享长图"（导出图片，走系统分享面板）+ "查看完整明细"（跳转统计页对应月份） |
| 视觉强度 | 走DESIGN_SYSTEM.md的"仪式层"满格强度（撞色背景+贴纸装饰），是全App视觉冲击力最强的一屏 |
| 简洁模式下的行为 | 不自动弹出提示条，仅在统计页保留一个低调的"月度总结"入口，内容仍生成但呈现克制（去掉称号徽章和贴纸装饰，只保留四宫格数据） |

### 3.10 成就徽章墙（我的页子屏幕）——已实现

| 组件 | 说明 |
|---|---|
| 顶部 | 当前称号横幅（含吉祥物待机动效）+ 距下一级还差多少小时的进度条 |
| **称号进阶之路**（规划外新增，比原计划更游戏化） | 5个称号节点按"Z字形"蜿蜒路径排布（参考多邻国技能树），节点间用连接线绘制（当前进度之前的线段高亮），当前所在节点有呼吸光晕脉冲动画+搭子吉祥物悬浮在节点上方；锁定节点为虚线圆圈，已解锁为实线+渐变底色；节点按索引交错入场动画 |
| 隐藏成就徽章网格 | 4个彩蛋成就：双开达人（同天为≥2个副本打卡）/ 不灭火苗（连续打卡满30天）/ 深夜战士（累计10次22点后打卡）/ 省钱达人（连续N周达成周收入目标）；已解锁彩色点亮，未解锁灰色轮廓+锁图标，**均显示名称和解锁条件**（不做完全隐藏） |
| 交互 | 点击任意徽章/称号节点弹出详情卡片，说明解锁条件和当前状态；卡片/节点均有点击缩放的按压反馈 |

### 3.11 空状态清单

产品首次使用/数据清空时，每个关键屏幕都需要专门设计空状态，不能留白报错或显示"暂无数据"这种敷衍文案：

| 屏幕 | 空状态文案与操作 |
|---|---|
| 首页（未添加任何雇主） | 吉祥物插画 + "还没有雇主？点击右下角开始你的搬砖之旅" + 引导按钮直达3.2添加雇主表单 |
| 统计页（无工时记录） | "打完第一次卡，这里就会出现你的战绩" |
| 净收益对比页（雇主数<2） | "至少添加2个雇主才能开始比较，去添加一个？"+ 引导按钮 |
| 成就徽章墙（全新用户） | 所有徽章都是未解锁灰色态，顶部文案"从萌新打工人开始你的进阶之路" |
| 团队人员列表（组长模式，未添加任何人） | "还没有需要代记录的人？点击添加第一位" |

---

## 3.12 屏幕跳转流程图

给UI设计和开发理清屏幕之间的跳转关系，避免各画各的导致断层：

```
[启动页]
   └─(未登录)→ [登录页 3.0] → [首次引导3屏] → [添加雇主表单 3.2] → [首页 3.1（含新手数据）]
   └─(已登录)→ [首页 3.1]

[首页 3.1]（空状态见3.11）
   ├─ 点雇主行"上班/下班打卡" → [打卡确认卡片 3.3]（含心情emoji）→ 返回首页
   ├─ 长按"+"浮动按钮 → 快捷菜单：[补录表单 3.3] / [拍照识别 3.7] / [语音记工 3.7]
   ├─ 点"+"添加雇主 → [添加雇主表单 3.2] → 保存后弹出入职动画 → 返回首页
   ├─ 点顶部称号横幅 → [成就徽章墙 3.10]
   ├─ 月度总结提示条 → [月度战绩总结 3.9]
   └─ 底部Tab → [统计页 3.4] / [我的页 3.6]

[统计页 3.4]（空状态见3.11）
   ├─ 点导出 → 导出弹层（Excel/PDF）
   ├─ 点"打工战绩总结"卡片 → [月度战绩总结 3.9]
   └─ 点明细列表某条 → 编辑该条记录（复用3.3表单，预填数据）

[月度战绩总结 3.9]
   └─ "生成分享长图" → 系统分享面板（微信/小红书等）

[我的页 3.6]
   ├─ 点个人卡片 → [成就徽章墙 3.10]
   ├─ 点"团队"入口（组长模式） → [团队人员列表 3.8] → [为某人记工（复用3.3）] / [团队统计（复用3.4）]
   ├─ 点"净收益对比" → [净收益对比页 3.5]（空状态见3.11）
   └─ 点各设置项 → 对应设置弹层/子页

[系统推送：每日小结 3.13] → 点击 → [统计页 3.4]（当天明细）
```

### 3.13 每日轻量小结（推送通知）——🟡 部分实现，非阻塞待办

设置页开关（"每日小结推送"，默认开启）已经做好，但**触发推送本身的后端定时逻辑尚未接入**（需要Cloud Functions定时任务/触发器 + Firebase Cloud Messaging，或小程序端对应的订阅消息能力），目前这个开关不产生实际推送效果。以下规格保持不变，作为后续接入推送时的实现依据：

**调研结论**（详见需求来源）：Duolingo式"连续打卡中断惩罚"推送不适合零工场景（零工是自由选择上下班，暗示"没打卡=失败"容易被视为道德绑架）；Toggl/RescueTime证明工具型App用户对被动推送容忍度低；周推送≥5条会导致64%用户卸载App。GigTime的每日推送必须走"轻量确认"路线，不做情感绑架。

| 规则 | 说明 |
|---|---|
| 触发时机 | 检测到当天**最后一次下班打卡后30-60分钟**推送，不用固定时间点（尊重每个人的下班时间不同） |
| 触发条件 | **仅当天有打卡记录才发**；没上班的日子不推送、不打扰 |
| 频率上限 | 每天最多1条，不叠加其他营销/提醒类推送 |
| 内容 | 极简三件套：今日工时 + 今日收入 + 本周累计收入，不做多维度报表 |
| 文案示例 | "今天搬了6.2小时的砖，到账¥173.6，本周已经攒了¥712啦" |
| 关闭入口 | 设置页"打卡设置"分组下新增开关"每日小结推送"，默认开启但一键可关，不做二次挽留弹窗 |
| 周报/月报 | 复用3.9月度战绩总结的叙事化仪式感，但"仪式感"只留给周/月这类低频节点，每日推送必须克制，避免"天天都是Wrapped"导致脱敏 |

**技术实现提醒**：判断"最后一次下班打卡后30-60分钟"需要一个延时触发的后端逻辑（Cloud Functions定时任务或Firestore触发器）+ Firebase Cloud Messaging推送。Firebase Cloud Functions（2nd gen）需要绑定Blaze按量付费方案才能部署（即使实际用量在免费额度内也需要先挂载billing account），这点和此前登录方式讨论中的"0成本"张力类似，属于开发这个功能前需要你知晓并接受的一个小前提，不是本次需要决策的阻塞项。

### 3.14 心情标签（已实现，最终规格比原计划更丰富）

**调研结论**：Daylio等心情记录App证明"纯标签点选"留存率最好，强制长文写作留存率最差；工具型App加情绪功能必须轻到不像独立功能，且不能做成社交（点赞/广场/好友可见），否则会造成产品定位分裂。

**⚠️ 与最初规划的差异**：原计划是4档标签（崩溃/普通/爽/心动），实际开发中扩充为**8档**，每档配一个专属手绘表情图标（`MoodIcon`组件，SVG路径手绘而非emoji/图库）+专属固定色值，覆盖更细腻的情绪光谱：

| 标签 | 色值 | 表情设计要点 |
|---|---|---|
| 崩溃 crash | `#5AC8FA` | 晕眩眼+波浪嘴 |
| 社畜 ox | `#A89078` | 眼袋下垂+扁平嘴 |
| 躺平 flat | `#C7C2B8` | 闭眼歪头+ZZZ |
| 普通 normal | `#B9AC9C` | 圆点眼+平嘴 |
| 摸鱼 slack | `#8AC9B8` | 半眯眼+得意笑+尾巴 |
| 爽 great | `#FFD93D` | 弯眼笑+张嘴笑 |
| 爆肝 grind | `#FF8C42` | 怒眉+咬牙齿+火花 |
| 心动 heartbeat | `#FF6B6B` | 心形轮廓，无五官 |

| 规则 | 说明 |
|---|---|
| 追加文字 | 点选标签后，下方浮现一个可选的单行文字框，**上限20字**，不强制填写；不做多行、不做语音输入 |
| 嵌入位置 | **打卡确认卡片**（3.3）+ **补录/编辑工时表单**（App端与规划一致仅打卡确认卡片有；小程序端补录表单也接入了心情选择，与"严肃场景不放情绪标签"的原则有出入，属于开发中的范围放宽，非bug）+ **AI记工确认页**（拍照/语音识别结果确认时也可选心情） |
| 展示方式 | 统计页/我的页有"本周心情曲线"卡片：7个点按心情值Y轴分布连成折线，带4条参考网格线，仅自己可见；月度战绩总结沿用"心情标记次数最多的一天"逻辑，无标记时兜底显示"当天工时最长的一天" |
| 心情×收入洞察 | 我的页心情曲线卡片会额外提示"周X心情是「Y」，也是这周赚得最多的一天"，把心情数据和收入数据做了轻量关联展示（规划外新增） |
| 搬砖搭子联动 | 首页悬浮的宠物/搭子小组件会根据**当天最新一次心情记录**切换专属动画（每个心情一套独立的CSS/SVG动效：崩溃是倒地抽搐、爽是弹性跳跃、心动是双重心跳缩放、爆肝是快速左右旋转等），是心情数据除了图表之外的第二个可视化出口（规划外新增，是本产品情绪化设计的重要延伸） |
| 明确不做 | 不做点赞、不做公开广场、不做好友可见、不做基于心情的社交推荐——这些都是工具型App加社交属性后定位分裂的已知坑，此原则未变 |

---

## 4. 交互原则（贯穿所有页面）

1. 涉及金额/时长的最终提交前，一律先展示"确认卡片"，不做静默保存，降低误操作风险
2. AI识别类功能（OCR/语音）的结果一律是"草稿"，需用户手动确认才写入正式记录，不自动覆盖
3. 定位相关权限一律默认关闭，需用户主动去设置页开启，且开启时用文案说明"仅工作时段生效"
4. 颜色标签用于区分雇主，贯穿首页、统计图表、明细列表，保持一致的视觉映射
5. 核心记录路径（打卡/补录）无论加多少趣味元素，操作步数和响应速度不能被拖慢；所有"好玩的东西"都必须能在设置里一键关闭（对应0.6的简洁模式）

---

## 5. 文案示例库（供UI/文案设计直接取用）

### 5.0 写作原则（避免"千篇一律"的具体做法）

1. **每类场景至少准备2-3个变体轮换展示**，不要让用户天天看到同一句话——尤其是首页收入类比、深夜打卡提醒这类高频触发的文案，写作时直接产出一个"文案池"，前端随机/轮换选取
2. **梗要具体、要与时俱进，别用停留在过去的老梗**：定期（建议每季度）回顾一次文案库，替换掉已经过时或用户开始无感的表达，参考当下小红书/微博打工人话题的真实说法，而不是套用几年前的流行语
3. **数字要具体，不要空泛抒情**：宁可写"够加7杯奶茶"也不要写"你真棒"这种没有信息量的夸奖，具体的类比才有记忆点和分享欲
4. **自嘲不代表贩卖惨**：语气始终是"苦中作乐的胜利者视角"，不是真的在诉苦——这是这套人设能被年轻人喜欢而不是被嫌矫情的关键分寸

### 5.1 通用场景

| 场景 | 示例文案（每类给2-3个变体，实际使用时随机轮换） |
|---|---|
| 首页收入类比 | "本月已搬砖87小时，相当于连续刷了29部电影"／"这周合计赚了¥520，够加15次奶茶"／"今天挣的钱，够点一份麻辣烫加蛋加面" |
| 深夜打卡（22点后） | "卷到这么晚，牛马也要注意身体"／"这个点还在打工？懂的都懂"／"打工人打工魂，但也别熬夜熬成鬼" |
| streak中断 | "火苗熄了，牛马也要按时上班哦"／"昨天摸鱼去了？没事，今天重新点火" |
| 净收益对比 | "你的实际到手时薪是¥18.3/小时，比隔壁摸鱼的猫少一点"／"算上通勤，这份工作其实没有看起来那么香" |
| 新雇主添加成功 | "新雇主 [名称] 入职啦"／"[名称] 解锁，打工版图又扩张了" |
| 称号体系（按累计工时/打卡天数升级） | 萌新打工人 → 摸鱼练习生 → 搬砖能手 → 卷王候选人 → 牛马之王 |
| 月度总结卡片标题 | "本月打工战绩报告"／"这个月，你的搬砖成绩单" |
| 空状态（未添加雇主时） | 小牛吉祥物插画 + "还没有雇主？点击右下角开始你的搬砖之旅"／"打工人的第一步：先加一个雇主" |
| 按单计费模式专属类比 | "今天跑了23单，比昨天多接了5单"／"这一单的钱，够加半份油" |
| 环形目标达成 | 未达成："还差¥120就能碰到本周目标啦"；达成时："目标达成！这周的KPI你自己给自己发了"（配小庆祝动效） |
| 热力日历高强度月份 | "这个月的格子红得发烫，牛马是你没错了" |
| 雇主排行榜第一名 | "本周最赚钱雇主：[雇主名]，遥遥领先" |

### 5.2 每日小结推送（对应3.13，务必轮换，避免"每天同一句话"引发推送疲劳）

- "今天搬了6.2小时的砖，到账¥173.6，本周已经攒了¥712啦"
- "打卡收工！今天入账¥173.6，继续攒钱倒计时"
- "辛苦一天，¥173.6到账，本周进度条又往前挪了一截"

### 5.3 心情标签反馈文案（对应3.14，选中标签后的一句轻反馈）

| 标签 | 反馈文案示例 |
|---|---|
| 崩溃 | "记下了，今天辛苦了，摸鱼一下犒劳自己吧" |
| 普通 | "平平淡淡才是打工的日常" |
| 爽 | "今天状态不错，这种日子多来点" |
| 心动 | "记录一下这个瞬间，打工也有小确幸" |

---

## 6. 下一步

本文档可直接用于Figma等工具生成线框图/高保真设计稿。建议设计顺序：首页（含吉祥物/称号横幅）→ 添加雇主表单（图标卡片选择）→ 打卡确认卡片（含心情标签）→ 统计页（含多样化数据可视化+打工战绩总结卡片）→ 设置页（含简洁模式）→ AI录入流程（最后做，技术复杂度最高）。

设计时建议先出两版首页视觉稿对比：一版"满血游戏化"（吉祥物+称号+streak全开），一版"简洁模式"，用于验证目标用户对游戏化元素的接受度，避免过度设计。
